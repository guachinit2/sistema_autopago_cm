# docker-compose.prod.yml - Imágenes de producción
# Uso: docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml up -d

services:
  backend:
    build:
      target: production

  frontend:
    build:
      target: production

  socket-server:
    build:
      target: production
